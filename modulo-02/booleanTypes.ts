// 01
let taskEnd: boolean = true
let taksPending: boolean = false

console.log(taskEnd)
console.log(taksPending)

// 02
let taksCompleted: boolean = false

if (taksCompleted) {
  console.log('tarefa concluída com successo!')
} else {
  console.log('tarefa pendente!')
}